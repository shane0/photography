# 2025

- trying galleria twelve theme
- adding giscus
- troubleshooting giscus works locally but not in action pipeline

## tshooting fresh repo

```sh
git init
git remote add origin git@github.com:shane0/photography.git
git branch -M main
git push -u origin main
```

## utility

- [ ] later move these to plugins/
- mover moving files into flat directory
- stringmover prompt for string move files based on name useful when using albums

## test new docs/ folder

- and cleanup

```sh
git rm -r --cached _build
# git push origin --delete gh-pages
# git push origin :gh-pages
rm -f .git/index.lock
```

## big file

```sh
find shane_null/ -type f -exec du -h {} + | sort -rh | head -n 1

```

## run locally

```sh
vv
cli p b
# my alias for venv is vv
# if new folder vvc && rrr && vv 
# one time sigal init
# sigal build
# sigal serve
# http://127.0.0.1:8000/
```

## setup

- note I remove the action so I can build a custom theme for giscus

```sh
mkdir -p .github/workflows && touch .github/workflows/deploy.yml
```

1g limit
du -h .

## garbage

```sh
# echo .DS_Store >> .gitignore
# git rm -r --cached .DS_Store

find ~ -name .DS_Store -delete
find . -name .DS_Store -delete

# git config --global core.excludesfile ~/.gitignore_global
# echo .DS_Store >> ~/.gitignore_global
```

## comments

- adding comments
- works locally but not deployed?

```sh
cli p 
# find
# python -c "import sigal; print(sigal.__file__)"
# /Users/shanenull/photography/venv/lib/python3.9/site-packages/sigal/
# code /Users/shanenull/photography/venv/lib/python3.9/site-packages/sigal/
# pip install --force-reinstall <package_name>

```
